# Markdown example

This folder demonstrates how you can use Kanel to generate documentation of your data model.

The [configuration file](./kanel.config.js), combined with the templates in the `docs-src` folder generate the result which you can inspect in the `docs` folder: [index](./docs/index.md)
